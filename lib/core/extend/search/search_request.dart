// import 'package:flutter/material.dart';

// enum RequestType { New, Refresh }

// class SearchRequest {
//   final String key;
//   final RequestType type;

//   SearchRequest({this.key, this.type});

//   bool get isRefresh => type == RequestType.Refresh;

//   factory SearchRequest.New({required String key}) =>
//       SearchRequest(key: key, type: RequestType.New);

//   factory SearchRequest.Refresh({required String key}) =>
//       SearchRequest(key: key, type: RequestType.Refresh);
// }
